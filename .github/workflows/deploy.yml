# .github/workflows/deploy.yml
# 部署到GitHub Pages工作流

name: Deploy to GitHub Pages

# 触发条件
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
  repository_dispatch:
    types: [trigger_deployment]

# 权限设置
permissions:
  contents: read
  pages: write  # 写入GitHub Pages
  id-token: write

# 并发控制
concurrency:
  group: "pages"
  cancel-in-progress: false  # 不取消正在进行的部署

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    # 步骤1: 检出代码
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    # 步骤2: 配置Pages环境
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    # 步骤3: 构建和测试网站
    - name: Build and test website
      run: |
        echo "正在构建和测试网站..."
        
        # 检查必要文件是否存在
        REQUIRED_FILES=("index.html" "js/main.js" "css/style.css" "css/responsive.css")
        for file in "${REQUIRED_FILES[@]}"; do
          if [ ! -f "$file" ]; then
            echo "❌ $file 不存在"
            exit 1
          fi
          echo "✅ $file 存在"
        done
        
        # 检查 JS 模块文件
        JS_MODULES=("dataLoader.js" "filter.js" "tableRenderer.js" "statistics.js")
        for module in "${JS_MODULES[@]}"; do
          if [ ! -f "js/$module" ]; then
            echo "❌ js/$module 不存在"
            exit 1
          fi
          echo "✅ js/$module 存在"
        done
        
        # 检查 homework_data.json 是否存在，如果不存在则创建一个空的
        if [ ! -f "homework_data.json" ]; then
          echo "⚠️ homework_data.json 不存在，创建空文件"
          echo "[]" > homework_data.json
        fi
        
        # 验证 JSON 格式
        if python -c "import json; json.load(open('homework_data.json'))" 2>/dev/null; then
          RECORD_COUNT=$(python -c "import json; data = json.load(open('homework_data.json')); print(len(data))")
          echo "✅ homework_data.json 格式正确"
          echo "📊 数据记录数量: $RECORD_COUNT"
        else
          echo "❌ homework_data.json 格式错误，创建空文件"
          echo "[]" > homework_data.json
        fi
        
        # 创建部署信息文件
        cat > deploy-info.json << EOF
        {
          "build_time": "$(date -Iseconds)",
          "commit_hash": "${{ github.sha }}",
          "data_records": $RECORD_COUNT,
          "deploy_type": "${{ github.event_name }}"
        }
        EOF
        
        echo "✅ 网站构建和测试完成"
        
    # 步骤4: 上传Pages制品
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: .  # 上传整个当前目录
        name: github-pages

  # 部署任务（依赖于构建任务）
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}  # 部署后的页面URL
    
    runs-on: ubuntu-latest
    needs: build  # 依赖build任务
    
    steps:
    # 步骤5: 部署到GitHub Pages
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
