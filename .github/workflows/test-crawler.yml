# .github/workflows/test-crawler.yml
name: Test Homework Crawler

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-crawler:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f "requirements.txt" ]; then
          pip install -r requirements.txt
        else
          pip install requests beautifulsoup4 pandas openpyxl colorama
        fi
        
    - name: Check syntax and imports
      run: |
        python -m py_compile homework_crawler.py
        echo "✅ 语法检查通过"
        
    - name: Verify file structure
      run: |
        echo "检查项目文件结构..."
        
        # 检查主要文件
        [ -f "homework_crawler.py" ] && echo "✅ homework_crawler.py 存在" || echo "❌ homework_crawler.py 不存在"
        [ -f "index.html" ] && echo "✅ index.html 存在" || echo "❌ index.html 不存在"
        [ -f "css/style.css" ] && echo "✅ css/style.css 存在" || echo "❌ css/style.css 不存在"
        [ -f "css/responsive.css" ] && echo "✅ css/responsive.css 存在" || echo "❌ css/responsive.css 不存在"
        
        # 检查 JS 文件
        JS_FILES=("main.js" "dataLoader.js" "filter.js" "tableRenderer.js" "statistics.js")
        for js_file in "${JS_FILES[@]}"; do
          [ -f "js/$js_file" ] && echo "✅ js/$js_file 存在" || echo "❌ js/$js_file 不存在"
        done
        
    - name: Test crawler functionality
      run: |
        echo "测试爬虫功能..."
        # 测试爬虫的帮助功能
        python homework_crawler.py --help && echo "✅ 帮助命令正常" || echo "⚠️ 帮助命令异常"
        
    - name: Create test report
      if: always()
      run: |
        echo "# 爬虫测试报告" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**测试时间:** $(date)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ 语法检查通过" >> $GITHUB_STEP_SUMMARY
        echo "✅ 依赖安装成功" >> $GITHUB_STEP_SUMMARY  
        echo "✅ 文件结构检查完成" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "📝 **说明:** 完整的功能测试需要有效的门户网站账号" >> $GITHUB_STEP_SUMMARY