# .github/workflows/update-data.yml
name: Update Homework Data

on:
  schedule:
    - cron: '0 9 * * *'  # 每天 UTC 09:00 (香港时间 17:00)
  workflow_dispatch:
    inputs:
      force_full_update:
        description: '强制完整更新'
        required: false
        default: false
        type: boolean

# 设置全局权限
permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    # 为这个job单独设置权限
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # 使用具有写入权限的token
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
        
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 pandas openpyxl colorama
        
    - name: Create config file
      run: |
        # 创建配置文件，使用GitHub Secrets中的凭据
        if [ -n "${{ secrets.PORTAL_USERNAME }}" ] && [ -n "${{ secrets.PORTAL_PASSWORD }}" ]; then
          cat > portal_config.json << EOF
        {
          "username": "${{ secrets.PORTAL_USERNAME }}",
          "password": "${{ secrets.PORTAL_PASSWORD }}",
          "last_updated": "$(date -Iseconds)"
        }
        EOF
          echo "✅ 配置文件创建成功"
        else
          echo "❌ GitHub Secrets中未设置PORTAL_USERNAME和PORTAL_PASSWORD"
          exit 1
        fi
        
    - name: Run homework crawler
      id: crawler
      run: |
        echo "开始运行家课数据爬虫..."
        python homework_crawler.py
        
        # 检查是否生成了数据文件
        if [ -f "homework_data.json" ]; then
          echo "数据文件生成成功"
          
          # 验证JSON格式
          if python -c "import json; json.load(open('homework_data.json'))"; then
            RECORD_COUNT=$(python -c "import json; data = json.load(open('homework_data.json')); print(len(data))")
            echo "记录数量: $RECORD_COUNT"
            echo "data_updated=true" >> $GITHUB_OUTPUT
            echo "record_count=$RECORD_COUNT" >> $GITHUB_OUTPUT
          else
            echo "❌ homework_data.json 格式错误"
            echo "data_updated=false" >> $GITHUB_OUTPUT
          fi
        else
          echo "❌ 数据文件生成失败"
          echo "data_updated=false" >> $GITHUB_OUTPUT
        fi
        
    - name: Debug - Check files
      if: steps.crawler.outputs.data_updated == 'false'
      run: |
        echo "调试信息:"
        ls -la
        [ -f "portal_config.json" ] && echo "✅ portal_config.json 存在" || echo "❌ portal_config.json 不存在"
        [ -f "homework_data.json" ] && echo "✅ homework_data.json 存在" || echo "❌ homework_data.json 不存在"
        
    - name: Create empty data file if failed
      if: steps.crawler.outputs.data_updated == 'false'
      run: |
        echo "创建空的homework_data.json文件..."
        echo "[]" > homework_data.json
        echo "data_updated=true" >> $GITHUB_OUTPUT
        echo "record_count=0" >> $GITHUB_OUTPUT
        
    - name: Create last update info file
      run: |
        # 创建最后更新时间文件
        cat > last_update.json << EOF
        {
          "last_updated": "$(date -Iseconds)",
          "workflow_run_id": "${{ github.run_id }}",
          "workflow_run_number": "${{ github.run_number }}",
          "triggered_by": "${{ github.event_name }}",
          "timestamp": "$(date +%s)"
        }
        EOF
        echo "✅ 最后更新时间文件创建成功"
        
    - name: Configure Git
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
    - name: Check for changes
      id: git-check
      run: |
        echo "检查文件变更..."
        git add homework_data.json homework_data.xlsx last_update.json
        if git diff --staged --quiet; then
          echo "has_changes=false" >> $GITHUB_OUTPUT
          echo "没有检测到文件变更"
        else
          echo "has_changes=true" >> $GITHUB_OUTPUT
          echo "检测到文件变更"
        fi
        
    - name: Commit and push changes
      if: steps.git-check.outputs.has_changes == 'true'
      run: |
        echo "提交并推送变更..."
        # 获取当前日期并格式化
        CURRENT_DATE=$(date +"%Y-%m-%d")
        COMMIT_MESSAGE="📚 自动更新家课数据 (${{ steps.crawler.outputs.record_count }} 条记录) - 更新日期: $CURRENT_DATE [skip ci]"
        git commit -m "$COMMIT_MESSAGE"
        git push
        
        # 触发部署工作流
        echo "触发部署工作流..."
        
    - name: Trigger deployment
      if: steps.git-check.outputs.has_changes == 'true'
      uses: actions/github-script@v6
      with:
        script: |
          try {
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy-to-pages.yml',
              ref: 'main'
            });
            console.log('✅ 已成功触发部署工作流');
          } catch (error) {
            console.log('⚠️ 触发部署工作流失败，但数据已更新');
            console.log('错误信息:', error.message);
          }
        
    - name: Create summary
      if: always()
      run: |
        echo "# 家课数据更新报告" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**运行时间:** $(date)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "${{ steps.crawler.outputs.data_updated }}" = "true" ]; then
          echo "✅ **状态:** 数据更新成功" >> $GITHUB_STEP_SUMMARY
          echo "📊 **记录数量:** ${{ steps.crawler.outputs.record_count }} 条" >> $GITHUB_STEP_SUMMARY
          echo "🔄 **触发方式:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "👤 **手动触发用户:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ steps.git-check.outputs.has_changes }}" = "true" ]; then
            echo "🚀 **部署状态:** 已触发自动部署" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ **部署状态:** 无数据变更，无需部署" >> $GITHUB_STEP_SUMMARY
          fi
        else
          echo "❌ **状态:** 数据更新失败" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ 请检查凭据配置和网络连接" >> $GITHUB_STEP_SUMMARY
          echo "🔍 **调试信息:**" >> $GITHUB_STEP_SUMMARY
          echo "- 确保 PORTAL_USERNAME 和 PORTAL_PASSWORD 已在 GitHub Secrets 中设置" >> $GITHUB_STEP_SUMMARY
          echo "- 检查门户网站是否可以正常访问" >> $GITHUB_STEP_SUMMARY
          echo "- 验证登录凭据是否正确" >> $GITHUB_STEP_SUMMARY
        fi
